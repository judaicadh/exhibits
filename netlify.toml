[build]
  command = "npm run build"
  publish = "dist"

# Preserve the previously published (misspelled) URL for SEO / inbound links.
[[redirects]]
  from = "/hebraica-vertias/*"
  to = "/hebraica-veritas/:splat"
  status = 301
  force = true
